type User {
  id: ID! @id
  name: String!
  email: String! @unique
  post: [Post!]!
}

type Post {
  id: ID! @id
  titile: String!
  body: String!
  published: Boolean!
  author: User!
}

type Comment {
  id: ID! @id
  text: String!
  author: User!
  post: Post!
}
